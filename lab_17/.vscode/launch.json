{
  "version": "0.2.0",
  "configurations": [
    {
      "name": "Run with clean",
      "type": "cppdbg",
      "request": "launch",
      "program": "${workspaceFolder}/main",
      "preLaunchTask": "build",
      "postDebugTask": "clean",
      "cwd": "${workspaceFolder}",
      "MIMode": "gdb",
      "miDebuggerPath": "/usr/bin/gdb"
    }
  ]
}
